# 04-样例仓库清单

## 1. 目的
用于验证 URL 跳转命中率与稳定性，支撑 `>=95%` 验收目标。

来源：`docs/prd.md`《4.验收标准》

## 2. 样例来源范围
根目录：`/Users/professor/workspace/company/git`

筛选原则：
1. 包含 Java Spring Web 接口定义。
2. 优先包含多模块项目。
3. 排除纯前端、无接口项目。

来源：`docs/prd.md`《4.验收标准》

## 3. 样例记录模板

每个项目建议按如下格式记录：

```markdown
### 项目：<项目名>
- 路径：<绝对路径>
- 模块：<模块名>
- 接口样例：
  - 输入：`/user/add` -> 期望：`UserController#add`
  - 输入：`/user/update` -> 期望：`UserController#update`
- 结果：通过/失败
- 备注：<失败原因或特殊路径规则>
```

## 4. 统计维度
1. 总样本数。
2. 命中成功数。
3. 命中失败数。
4. 失败分类：解析失败 / 匹配失败 / 跳转失败。

## 5. 当前状态
当前文档仅提供清单规范，待开始联调时补全具体项目明细。
