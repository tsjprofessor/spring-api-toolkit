# 03-待确认问题清单

## 1. 已确认决议

### 1.1 Kotlin 验收口径
结论：Kotlin 按与 Java 同等验收口径执行。
来源：用户确认（2026-04-26）
状态：已确认

### 1.2 多命中处理
结论：输入 `/user/add` 命中多个结果时，展示多个候选，由用户选择后跳转。
来源：用户确认（2026-04-26）
状态：已确认

### 1.3 URL 标准化
结论：需要处理 `context-path` 前缀、尾斜杠、重复斜杠、大小写。
来源：用户确认（2026-04-26）
状态：已确认

### 1.4 扫描范围
结论：不扫描测试代码；多模块需要扫描；默认仅扫描各模块 `src/main/java`。
来源：用户确认（2026-04-26）
状态：已确认

### 1.5 命中率与样例来源
结论：命中率要求 >= 95%；样例来源为 `/Users/professor/workspace/company/git` 下所有包含接口的项目。
来源：用户确认（2026-04-26）
状态：已确认

### 1.6 交互样式
结论：显示与交互参照用户提供的截图样式实现。
来源：用户确认（2026-04-26）
状态：已确认

### 1.7 多命中排序规则
结论：按以下规则排序并作为默认实现：
1. 完整路径精确匹配优先。
2. 路径规范化后精确匹配优先。
3. 前缀匹配优先于包含匹配。
4. 路径长度更短优先。
5. 最后按 `Controller#method` 字典序稳定排序。
来源：用户确认（2026-04-26）
状态：已确认

## 2. 仍待确认

当前无。
